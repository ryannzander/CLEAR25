# Slim requirements for Vercel (under 500MB Lambda limit)
# openpyxl is optional: not listed here so the app can run on JSON-only bundled_stations.json.
# Add openpyxl if you bundle Excel/NAPS files and need live coordinate merge from those files.
django>=4.2
requests
whitenoise
dj-database-url
psycopg2-binary
django-allauth
PyJWT
cryptography
httpx[http2]
django-cors-headers
