Analyze the methodology of the project (located in data/LAPTOP TSF 2026/01. Summary Documents/CLEAR_Methodology_ScienceFair Ver#1.pdf).

Do not guess about ANYTHING. Always refer to the data folder if needing context about a feature. If no context is found, ask me. Do not guess at all.

This is a RESEARCH project. It is very important that you make no mistakes while programming or leave any vulnerability.

After each feature I want you to git commit it. Make sure to make separate branches and such for a smooth and optimized workflow. DO NOT INCLUDE CURSOR IN YOUR COMMIT NAMES. Analyze previous commits and make a census whether or not to commit to a specific branch.

Any error that you make in the process should be dealt with as soon as possible. As well, I want you to update this specific file with any new discoveries/errors you have went through, and how to solve them.

---

## Explicit Data Paths (do not guess)

- **Methodology:** `data/LAPTOP TSF 2026/01. Summary Documents/CLEAR_Methodology_ScienceFair Ver#1.pdf`
- **Regression Excel:** `data/LAPTOP TSF 2026/07. The 4 Cities - Regression formulas and alert network stations/{City}/`
- **Validation:** `data/LAPTOP TSF 2026/10. Validation/`
- **Raw PM2.5 data:** `data/LAPTOP TSF 2026/03. RAW DATA PM25 CANADA+Provinces/`

---

## Git Workflow

- Create feature branches per phase (e.g., `feature/cursorrules-update`, `feature/data-layer-research`)
- Commit after each feature; avoid "Cursor" in commit messages
- Update this file's "Discoveries & Errors" section when encountering issues

---

## Discoveries & Errors

- **Research Excel structure:** Research files use a single sheet (e.g. Toronto_ALL_Regression_ON_QC), headers on row 4, columns: Station ID, City, Distance (km), Direction, Tier, R, Slope, Intercept. No separate "All Stations Data" sheet for lat/lon. Coordinates are loaded from Canada NAPS Stations file (05. NAPS Stations/04. Canada_NAPS_Stations_Active_Years.xlsx) for NAPS IDs; US EPA station IDs get coords from legacy Excel if available, else remain None (WAQI live fetch will skip them).
- **Station filtering:** R ≥ 0.30 (methodology 2.3); skip non-numeric station IDs (header rows like "Rule 3 Québec Stations").
- **Thunder Bay (Rule 2):** NAPS 60807, 60809 injected into Toronto network; coords from NAPS file. Used for trigger only (>35 µg/m³); weak correlation with Toronto per methodology.
- **Public-facing numbers must match the methodology (fixed 2026-05-23):** The landing page shipped with figures found in NO project doc — "97.9% detection", "0% false alarms", "6–48hr" as a stat, "20 years", "218 stations", and "83 smoke events" (the "83" was a misread of **83.3%** = 2024 precision), plus per-city detection rates. Canonical source = `CLEAR_Methodology_ScienceFair Ver#1` + `CLEAR_Performance_Reference.docx` (matches the dashboard Research tab). **Historical (2003–2023, 21 seasons, 33 events):** 90.9% accuracy, 100% sensitivity (0 missed), 89.7% precision, 15.7h avg / 87h max lead, 36M+ obs, 309 NAPS + 131 U.S. EPA = 440 stations. **2024 out-of-sample (8 events):** 87.5% accuracy, 83.3% precision, 24.7h avg lead. The system is **Toronto-validated**; the 4 cities are *target* cities. Verify other surfaces (README, app) before quoting stats.
- **Live map only showed ~10–20 stations (fixed 2026-05-24):** Two stacked causes. (1) **Map regression** — commit `223f98f` made `updateMapMarkers` render only `results` (WAQI-confirmed, ~10) instead of the full `stations` catalog. Fixed in [app-map.js](webapp/dashboard/static/dashboard/app-map.js): always render the full catalog (gray = no current reading), color stations that have a live result, fall back to results only if `/api/stations/` failed. (2) **Catalog collapse** — `RESEARCH_DATA_BASE`/`NAPS_STATIONS_PATH` in [settings.py](webapp/ews/settings.py) pointed at `data/LAPTOP TSF 2026/`, but the real research data was moved to `projdata/` (gitignored, commit `6b0284d`). With the Excel path dead, the app fell back to `bundled_stations.json`, which held only **31 real stations** padded at runtime with ~140 **synthetic fake-coordinate** stations (`_expand_bundled_stations`, since removed). Fixes: settings now probes `projdata/` then `data/LAPTOP TSF 2026/`; the synthetic padding is gone; `bundled_stations.json` is **regenerated from the real research Excel** via [scripts/gen_bundled_stations.py](scripts/gen_bundled_stations.py) → **201 stations** (4 cities + Thunder Bay Rule 2). 138 have exact NAPS coords baked in; the 63 US-EPA stations (no coord file) are placed at runtime in `load_stations()` via `_derive_coord()` from city-center + distance + 16-point compass direction (same construction as the polar maps), tagged `coord_source="derived"`. `waqi.py` skips `derived` stations in live matching so an approximate position is never attributed a real monitor's reading (they still render, just without a live value). Production has no `projdata` and no `openpyxl` (slim Vercel reqs) → it serves the regenerated JSON; derivation uses only stdlib `math`/`json`. **Regenerate with `python scripts/gen_bundled_stations.py --write` whenever the research Excel changes.**
- **US station coordinates are exact, from EPA AQS (added 2026-05-24):** The 63 US-EPA stations initially had no coordinate source (placed by distance+direction approximation, no live data). Fixed by pulling exact lat/lon from the public EPA AQS sites file (`https://aqs.epa.gov/aqsweb/airdata/aqs_sites.zip`, auto-downloaded by the generator to `scripts/aqs_sites.zip`, gitignored). Our 9-digit US IDs are State(2)+County(3)+Site(4) = the AQS site key — all 63 matched. Now **201 stations = 134 NAPS + 67 EPA, 0 derived**, all exact and all WAQI-eligible (so US stations get live data). `coord_source` is `"naps"` (Canadian, circle marker), `"epa"` (US, diamond marker), or `"derived"` (rare fallback, dashed diamond, skipped in WAQI). `_bundled_coord_lookup()` in data.py lets the research-Excel path reuse the bundle's exact coords (Excel has no lat/lon), so local == production.
- **Security hardening pass (branch `feature/security-hardening`, 2026-05-28):** Settings: fail-closed when `SECRET_KEY` is missing in non-DEBUG, default `DEBUG=false`, require `DATABASE_URL` in prod, refuse to boot with the dev secret, drop wildcard `*.vercel.app` CORS and replace with scoped `VERCEL_HOST_PREFIX`, set `SECURE_SSL_REDIRECT` + HSTS + Secure/SameSite=Lax cookies, raise `MinimumLengthValidator` to 12, allauth tightened (`mandatory` email verification, no login/logout-on-GET, throttles via `ACCOUNT_RATE_LIMITS`), `DATA_UPLOAD_MAX_MEMORY_SIZE` set to 1 MiB. New `dashboard/middleware.py` adds `RequestSizeLimitMiddleware`, `SecurityHeadersMiddleware` (CSP + Permissions-Policy + COOP + CORP), and per-IP `RateLimitMiddleware` for unauthenticated mutating routes. CSRF re-enabled on every session-auth view (account profile/delete, plan downgrade, key create/revoke, checkout, suggestions/comments/votes) — kept exempt only on HMAC-signed and CRON_SECRET-gated externals. `@ensure_csrf_cookie` on render views + a tiny `app-csrf.js` fetch shim attaches `X-CSRFToken` automatically. Webhook now validates amount/currency, refuses to re-process terminal-status payments (idempotent), and extends `plan_expires` using `max(current, now) + days`. `Payment` got `currency` + unique opaque `order_id` (migration 0015). `RefreshToken` got a non-null FK to `APIKey`; revoking a key cascades to revoke its tokens; `verify()` rejects tokens whose key is inactive. JWT endpoints throttled per-IP and per-key. `APIKey.check_rate_limit` is now an atomic `SELECT … FOR UPDATE` + `F()` so the limit can't be exceeded under concurrency. `api_test_upgrade` returns 404 unless `DEBUG`. `health_check` only returns per-component diagnostics when the `X-Healthcheck-Token` header matches `HEALTHCHECK_TOKEN`. Account deletion now requires typing `DELETE MY ACCOUNT` in the body (UI gates the button). Cron/secret string compares routed through `hmac.compare_digest` via `timing_safe_token_compare`. `manage.py check` and `check --deploy` both clean.
- **Grey live map — CSP blocked the tile server (fixed 2026-05-30, branch `fix/map-csp-tiles`):** The hardening pass's `SecurityHeadersMiddleware` CSP allowed only `https://*.tile.openstreetmap.org` in `img-src`, but [app-map.js](webapp/dashboard/static/dashboard/app-map.js) loads CARTO dark tiles from `https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png`. The browser silently blocked every tile request → grey background with only the `divIcon` station markers (inline HTML, unaffected by `img-src`) visible. **Fix:** add `https://*.basemaps.cartocdn.com` to the CSP `img-src` in [middleware.py](webapp/dashboard/middleware.py). **Lesson:** whenever a template/JS adds a new external host (tiles, fonts, scripts, XHR target), update the matching CSP directive — CSP failures are silent in the page and only show in the browser console.
- **Refresh cadence drifted to ~1.5–2h instead of 30 min (addressed 2026-05-30, branch `fix/refresh-cadence`):** Not a code bug — GitHub Actions *scheduled* workflows are best-effort and routinely delayed or **dropped** under load, worst of all at the congested top/bottom of the hour, which is exactly what `*/30 * * * *` targeted. **Mitigation:** schedule at staggered off-peak minutes (`9,39 * * * *`) to cut GitHub's drop rate while keeping 30-min nominal spacing and staying under the free private-repo Actions budget (~1,440 min/month vs 2,000 free; each run ~30s but billed ≥1 min, so do **not** raise frequency past ~30 min or add in-job `sleep` loops — both blow the budget on a private repo). `/api/refresh/` is idempotent (`CachedResult.update_or_create`), so late/duplicate fires are harmless. **For a guaranteed 30-min cadence** GitHub's scheduler cannot promise it on any plan; point a free external cron (cron-job.org / UptimeRobot / Cronitor) at `https://clear25.xyz/api/refresh/` with header `Authorization: Bearer <CRON_SECRET>` and keep this workflow as backup. The repo is **private** (confirmed via unauthenticated GitHub API → 404), which is why minute budget matters.
- **ECCC model ingestion — step 1: ingest + store only (branch `feature/eccc-ingest`, 2026-05-31):** Goal is to extend detection past the ~600 km station-regression ring by fusing CLEAR + ECCC models + PurpleAir (see the `eccc-purpleair-fusion-vision` memory). **This step adds ingestion+storage ONLY; it does NOT touch `evaluate.py` or `CachedResult(key="latest")`.** The fusion/alert rule is a deferred methodology decision. **ECCC MSC Datamart structure (verified live, do not guess filenames):** RDAQA hourly analysis at `https://dd.weather.gc.ca/today/model_rdaqa/10km/{HH}/` → files `{YYYYMMDD}T{HH}Z_MSC_RDAQA_PM2.5_Sfc_RLatLon0.09_PT0H.grib2` (also `RDAQA-FW_` and `RDAQA-Prelim_` preview variants — we use the plain final `_MSC_RDAQA_PM2.5_Sfc_`). RAQDPS 72h forecast at `.../model_raqdps/10km/grib2/{RUN}/{FFF}/` where RUN∈{`00`,`12`}, FFF=`000`–`072` → `{stamp}_MSC_RAQDPS_PM2.5_Sfc_..._PT{FFF}H.grib2`; **plus a dedicated `_MSC_RAQDPS_PM2.5-WildfireSmokePlume_Sfc_` product = surface PM2.5 from wildfire smoke only** (ideal for CLEAR; we sample both total + wildfire). **Discovery must be by the date/run stamp embedded in the FILENAME, never by directory-listing order** (ECCC doesn't guarantee order; yesterday's `22`/`23` folders linger in `/today`). The date+run prefix is constant across all forecast-hour folders of one run, so [scripts/eccc_ingest.py](scripts/eccc_ingest.py) reads it once from the `000` folder and synthesises the rest. **Architecture:** GRIB2 needs heavy native libs (`pygrib`/`eccodes`) that do NOT fit the slim Vercel lambda, so reading happens in a GitHub Actions runner (`.github/workflows/eccc-ingest.yml`, deps in [scripts/requirements-eccc.txt](scripts/requirements-eccc.txt) — never add these to `webapp/requirements*.txt`). The runner samples PM2.5 at the `bundled_stations.json` coords + 4 city centroids (nearest grid cell via a 3D unit-sphere KDTree, which handles the rotated `RLatLon0.09` grid correctly) and POSTs compact JSON to `/api/refresh/eccc/` (CRON_SECRET-gated, CSRF-exempt, idempotent), which writes `CachedResult(key="eccc_forecast"|"eccc_analysis")`. Read it back at `GET /api/eccc/?kind=forecast|analysis`. **Cadence/budget:** forecast updates only 2×/day (00Z/12Z, published ~3-4h later) and downloading its 73 files is the costly part, so the workflow runs every 3h with `--skip-if-current` (no-op when the stored run stamp already matches), keeping most runs to the single cheap analysis file. **Far-field virtual upwind sample points (the actual >600 km extension) are a `FAR_FIELD_POINTS = {}` stub** pending the sector/distance geometry decision. **Not yet validated end-to-end:** the grib-read + live-Datamart paths can only run in CI (no eccodes on the Win dev box); filename/stamp string logic and `manage.py check` are verified locally.
- **ECCC RAQDPS run-discovery bug + grib mask (fixed 2026-06-09, branch `feature/eccc-ingest`):** `find_latest_raqdps` chose the forecast run by the highest numeric folder id (`sorted(...)[-1]` → always `12`). But ECCC's `/today` reuses the fixed `00`/`12` folders and keeps the *previous day's* files in a run folder until that run republishes, so before today's 12Z lands the stale `12/` (yesterday) was preferred over today's fresh `00/` — and it returned `None` entirely when `12/` existed but had not yet posted PM2.5. **Fix:** pick the run by the `(date, hour)` stamp parsed from its `000`-hour filename, with fallback across runs (`_probe_raqdps_run`), mirroring `find_latest_rdaqa`. **Lesson reaffirmed:** discover by filename stamp, never by directory order *or run id*. Also `_GridSampler.sample` read `grbs[1].values` via `np.asarray(...)`, which drops a pygrib `MaskedArray`'s mask and would surface a fill value as real PM2.5; now `np.ma.filled(..., np.nan)` so the `isnan` guard rejects missing cells. (Verified live via HTTP directory listing of the Datamart that `00/` and `12/` both carried that day's stamp; grib read still CI-only.)
- **Profanity filter Scunthorpe bug (fixed 2026-06-09, branch `fix/input-validation`, PR → main):** `contains_profanity()` ran a bare `word in text` substring test alongside the `\b`-bounded regex, so legitimate input was rejected: "Cassidy"/"Bass"/"classic" (→ass), "Hancock" (→cock), "Dickson" (→dick), "Scunthorpe" (→cunt) — breaking real names in profile updates and words in suggestions/comments. **Fix:** keep only the word-boundary match (still catches standalone profanity + leetspeak via the existing `@$013` normalization); unit-tested. Same branch guards `push.send_alert_notifications` `:.1f` against a `None` PM2.5 (latent; no callers yet). **Reported, not changed:** the NOWPayments webhook's underpayment guard compares `price_amount` (our own echoed invoice price) to itself — illusory, but real protection is status-based (`partially_paid ∉ _TERMINAL_OK`); and `evaluate.ALERT_LEVELS` bands overlap at integer boundaries (`MODERATE.max=60`/`HIGH.min=60`) — left alone as the validated alert core.
- **Plan / Ontario smoke-plume tracker — PurpleAir 5-min GIS (branch `feature/plan-plume-tracker`, 2026-06-09):** New, fully **isolated** view (`/plan/`) — never touches `evaluate.py`, `CachedResult`, WAQI, or the alert engine. Decisions (user, 2026-06-09): source = **PurpleAir** for the dense 5-min signal (key fact: ECCC ground stations only update ~hourly — SWOB/AQHI — so true 5-min frames come only from PurpleAir; ECCC RDAQA grid is the chosen hourly reference layer, read from the existing `/api/eccc/?kind=analysis`); cleaning = **US-EPA / Barkjohn (2021)** correction `PM2.5 = 0.524·PAcf1 − 0.0862·RH + 5.75` + QA (`channel_flags==0`, `confidence≥80`, freshness ≤1h, range); GIS = **animated IDW heatmap surface** with a 5-min time slider. **PurpleAir API:** `GET https://api.purpleair.com/v1/sensors`, header `X-API-Key`, bbox `nwlng/nwlat/selng/selat`, `average=0` (real-time); **the response reorders columns and prepends `sensor_index`, so parse by the returned `fields` array, never request order** ([services/purpleair.py](webapp/dashboard/services/purpleair.py)). Ontario bbox is a rectangle so it also catches upwind US-Great-Lakes sensors at the corners (useful for approaching plumes). **Storage:** `PlumeFrame` model (migration 0016), one cleaned snapshot per row, **unique(source, captured_at)** so jittered cron fires are idempotent; ingest prunes rows older than `PLUME_BUFFER_HOURS` (6h). **Endpoints:** `POST /api/plan/refresh/` (CRON_SECRET-gated, CSRF-exempt, fetch→clean→store→prune — runs on Vercel since PurpleAir is plain JSON, no grib), `GET /api/plan/frames/` (public, slim `{lat,lon,pm}` by default, `?full=1` for raw/rh/conf), `/plan/` page. **CADENCE — must use an EXTERNAL cron, NOT GitHub Actions:** 5-min cadence = 288 runs/day; even billed at ≥1 min that is ~8,640 min/month, far over the 2,000 free private-repo budget (see the refresh-cadence note). Point a free external cron (cron-job.org / UptimeRobot) at `https://clear25.xyz/api/plan/refresh/` with header `Authorization: Bearer <CRON_SECRET>` every 5–10 min. **Secrets:** `PURPLEAIR_API_KEY` is read from env only (never committed; set it in Vercel + local `.env`). Verified live end-to-end (1403 sensors → ~1037 kept after QA; ingest/store/read/auth all green; IDW math unit-tested). **Frontend** ([app-plan.js](webapp/dashboard/static/dashboard/app-plan.js)) does IDW client-side onto a 150×110 grid → canvas → `L.imageOverlay` (uses `data:`/`blob:`, already allowed by CSP `img-src`); no new CSP hosts needed (PurpleAir is fetched server-side). **Browser-verified** (headless Chrome): dark basemap + IDW PM2.5 surface render over Ontario, all dynamic fields populate, no fatal JS errors.
- **Plan ECCC RDAQA model layer — "best version" (branch `feature/plan-plume-tracker`, 2026-06-09):** Adds the user's chosen ECCC reference as a true **gridded 10 km raster** surface, not sparse points. **Key constraint discovered:** `eccc_ingest.py` samples ONE point set for both the analysis AND the 73-h forecast, so a dense Ontario mesh on the shared set would blow the forecast payload past the 1 MiB ingest cap. **Solution:** an **analysis-only** Ontario mesh — `build_ontario_mesh()` (regular grid over `ONTARIO_BBOX`, 0.2° ≈ 76×105 = 7980 cells, row-major north→south/west→east, pure-Python so it's locally unit-testable) is sampled only in `ingest_analysis` (reusing the same `_GridSampler` KDTree) and stored compactly as `payload["mesh"] = {rows,cols,bbox,spacing,values[]}` (~43 KB flat array, nulls for missing). Forecast untouched. `--no-mesh` opts out. The frontend ([app-plan.js](webapp/dashboard/static/dashboard/app-plan.js)) reads `GET /api/eccc/?kind=analysis`, and a segmented **Observed · PurpleAir** (animated IDW) / **Model · ECCC RDAQA** control renders the mesh directly as a canvas raster `imageOverlay` (no IDW — it's already gridded). `/plan/?mode=model` deep-links the model surface. The Model button auto-disables when no mesh is ingested yet. **Verified:** mesh geometry unit-tested locally; endpoint→frontend→raster render browser-verified with a seeded synthetic mesh (the live grib sampling is CI-only, same constraint as all ECCC grib work, but reuses the already-validated `_GridSampler`). **`ONTARIO_BBOX` is duplicated in `scripts/eccc_ingest.py` and `services/purpleair.py` — keep them in sync.**
- **Fusion phase — read-only, isolated (2026-06-09/10):** First links between PurpleAir/ECCC and the CLEAR network, all READ-ONLY (never touch `evaluate.py` or any alert — fusing into the live 3-rule engine is deferred until backtested). [services/fusion.py](webapp/dashboard/services/fusion.py): (1) `confirmation_for_city` — median EPA-corrected PM2.5 of PurpleAir sensors within 60 km of a city (≥3 required) vs `CITY_ELEVATED_THRESHOLD` (20) → status confirmed/unconfirmed/purpleair_only/agree_calm/no_purpleair; (2) `early_warning_for_city` — reads the stored RAQDPS 72h forecast (`CachedResult(key="eccc_forecast")`), reports lead time to the city (centroid crossing 20) + upwind far-field origins (≥35, parsed from the `FAR:{city}-{brg}deg-{dist}km` ids) — the >600 km extension. `GET /api/plan/fusion/` returns both per city; [app-fusion.js](webapp/dashboard/static/dashboard/app-fusion.js) paints chips on the dashboard city cards. **Coverage caveat:** PurpleAir is Ontario-only, so confirmation only works for Toronto today; ECCC early warning covers all 4 cities once the forecast is ingested. **Merge-timing gotcha (recurred on #9/#10 and #11/#12):** the user merges PRs the instant they open, deleting the branch; later commits land on a recreated branch outside any PR. **Make each PR complete before opening it; if a follow-up is needed, open a NEW PR rather than pushing to the merged branch.**
- **PurpleAir historical pull for backtesting ([scripts/purpleair_history.py](scripts/purpleair_history.py), 2026-06-10):** Standalone (requests + stdlib; no app/DB). **PurpleAir history API (verified live):** `GET /v1/sensors/{id}/history?start_timestamp&end_timestamp&average&fields` → `{fields:[time_stamp,...], data:[[...],...]}` rows UNSORTED. Per-request span is capped by averaging interval (`0:2d,10:3d,30:7d,60:14d,360:90d,1440:365d`) so the script chunks long ranges. Data back to 2016 but **PurpleAir is only dense ~2018+** → backtest against recent events (2023/2024). **COST:** historical requests bill API "points" (own sensors free) — use `--list-only` (cheap discovery) first, `--max-sensors` to cap. Applies the same Barkjohn correction; writes one CSV with a `pm2.5_corrected` column. Output dir `purpleair_history/` is gitignored. Discovery + a 1-sensor/1-day pull verified live.
- **Plume tracker repurposed to local 2023 data — zero API cost (branch `feature/plume-2023-data`, 2026-06-20):** To end the live-API credit drain, `/plan/` now animates a **static local 2023 daily dataset** instead of the metered live API. Source = `data/PurpleAir Download 2026-06-19/` (gitignored): 523 per-sensor CSVs (`<id> 2023-01-01 2023-12-31 1440-Minute Average.csv`, columns `time_stamp,pm2.5_atm,latitude,longitude`, **daily**) + `station_coords.json`. **Only `pm2.5_atm` is present (no CF=1/RH), so the EPA/Barkjohn correction CANNOT be applied** — values are uncorrected ATM PM2.5. [scripts/gen_plume_2023.py](scripts/gen_plume_2023.py) (`--write`) compiles them into a compact committed static asset [webapp/dashboard/static/dashboard/plume_2023.json](webapp/dashboard/static/dashboard/plume_2023.json) (~900 KB: `{year,dates[365],bbox,stations[{id,lat,lon}],values[[pm|null x365]]}`, 522 stations, 120k points). [app-plan.js](webapp/dashboard/static/dashboard/app-plan.js) fetches it (no `/api/plan/frames/`, no `loadEccc`), expands one frame per day, **clips the surface to an Ontario + Québec boundary** ([provinces-boundary.js](webapp/dashboard/static/dashboard/provinces-boundary.js), DP-simplified ON+QC rings from click_that_hood — `clipRegion=true`, `inRegion`/`regionMask`) so the surface follows the two provinces instead of a bbox rectangle (US sensors still inform interpolation near the border but aren't drawn), date-labels the slider, and auto-opens on the **peak-smoke day (2023-06-28, the late-June wildfire event)**. Regenerate with `python scripts/gen_plume_2023.py --write` for a new download dir. Browser-verified: the June-2023 smoke surface renders over the Great Lakes. The live ingest endpoints/cost-guard (PR #15) still exist but the tracker no longer depends on them.
- **ECCC NAPS historical PM2.5 extraction ([scripts/gen_naps_2023.py](scripts/gen_naps_2023.py), 2026-06-20):** "ECCC historical data" = **NAPS reference station observations** (the RAQDPS/RDAQA *model* grids are NOT archived — the MSC Datamart is real-time only, ~last day; historical model output isn't publicly downloadable). NAPS files sit behind a JS catalogue at `data-donnees.az.ec.gc.ca`; its API (found by reading the SPA bundle) is `GET /api/path_contents?path=air/monitor/national-air-pollution-surveillance-naps-program/...` (list) and `GET /api/file?path=...` (download, **302 → blob, follow redirects**). 2023 hourly PM2.5 = `.../Data-Donnees/2023/ContinuousData-DonneesContinu/HourlyData-DonneesHoraires/PM25_2023.csv` (14 MiB). **Format:** metadata rows, then a header row starting `Pollutant//Polluant`, then data rows `Pollutant,MethodCode,NAPS_ID,City,Prov,Lat,Lon,Date,H01..H24` — **`-999` = missing, zeros valid; lat/lon embedded per row** (no station-join needed). The script averages hourly→daily (≥18 valid hours = NAPS 75% rule), filters to **ON+QC by default** (`--all-provinces` to override), and writes the **same compact format as `plume_2023.json`** to the **gitignored `naps_2023/`** (testing only — not promoted to `static/`). Result: **87 ON+QC reference stations, 30k daily points; peak-median 2023-06-25 = 99 µg/m³** (confirms the late-June wildfire smoke the PurpleAir data showed). Reference-grade → **no EPA correction**. To show NAPS in `/plan/`, copy `naps_plume_2023.json` into `static/dashboard/` and point the frontend at it (identical schema), or add a PurpleAir/NAPS layer toggle.
- **Smoke collector + dashboard for the Raspberry Pi ([smoke_pi/](smoke_pi/), 2026-06-23):** Self-archives wildfire-smoke MODEL output going forward (neither source keeps history publicly — see the FireWork/BlueSky findings: ECCC model grids aren't archived, BlueSky/firesmoke.ca retains only ~3 months). [collector.py](smoke_pi/collector.py) polls **BlueSky Canada** (`firesmoke.ca/forecasts/<BSCrrCA12-ver>/<YYYYMMDDHH>/dispersion.nc`, NetCDF, ~84 MB/run, ~4×/day) and **FireWork/RAQDPS** (the `PM2.5-WildfireSmokePlume_Sfc` GRIB2 product, 2×/day), **clips each to an Ontario+Québec 0.15° mesh** (tens of KB, not the raw grid), dedupes by run stamp, and appends a slice → `smoke_pi/web/data/slices/` (the future training set) + `latest_<src>.json` + `status.json` + `history.json`. [serve.py](smoke_pi/serve.py) (stdlib) serves the **dashboard** ([web/](smoke_pi/web/)) — a dark ops console (per-source status/sparklines, disk/archive meters, activity feed, live ON/QC smoke raster). systemd units + README included. The Pi is the right host because it reads GRIB2/NetCDF (the libs that don't fit Vercel). **Verified:** both sources' discovery resolve live; dashboard browser-verified with synthetic data. The grid SAMPLING (`sample_bluesky` via xarray, `sample_firework` via pygrib) is **Pi-validated only** (no eccodes/xarray on the Win box). **`web/data/` is gitignored.** **⚠️ DATAMART RESTRUCTURE (also breaks the live app):** ECCC moved the Datamart from `dd.weather.gc.ca/today/model_raqdps/10km/grib2/...` (now **404**) to date-partitioned `dd.weather.gc.ca/{YYYYMMDD}/WXO-DD/model_raqdps/10km/grib2/{HH}/{FFF}/`. The collector uses the new path (today→yesterday fallback), but **`scripts/eccc_ingest.py` still uses the dead `/today/` path and is now broken** — update it when next touched.
- **Plume tracker moved to 2024+2025 HOURLY PurpleAir, replacing 2023 (branch `feature/plume-2024-2025-data`, 2026-06-27):** `/plan/` now animates **two years of hourly** local PurpleAir data (still zero live-API cost at runtime). Source = `data/PurpleAir2024/PurpleAir Download 2026-06-26/` + `data/PurpleAir2025/...` (gitignored): ~536/510 per-sensor CSVs `<id> {YYYY}-01-01 {YYYY}-12-31 60-Minute Average.csv`. **KEY DIFFERENCE from the 2023 download: columns are ONLY `time_stamp,pm2.5_atm` — NO lat/lon, and there is NO `station_coords.json`.** So coordinates are fetched **once** from PurpleAir `GET /v1/sensors?fields=latitude,longitude&show_only=<comma ids>` (metadata only, not history → cheap; chunked ≤250 ids) by [scripts/gen_plume_2024_2025.py](scripts/gen_plume_2024_2025.py) `--fetch-coords` (needs `PURPLEAIR_API_KEY` env), cached to the **gitignored `scripts/pa_coords_cache.json`**; the committed `plume_2023.json` is a secondary coord fallback. Result: **595 sensors, 0 dropped, 4.70M points.** Still only `pm2.5_atm` → **no EPA/Barkjohn correction** (uncorrected ATM PM2.5). **Encoding:** hourly×2yr is too big for the 2023 dense matrix (~520×17,544 mostly-null) — instead **sparse station-keyed** (`{t0,step_seconds,n_steps,bbox,stations[{id,lat,lon}],series:[[hour,pm,...]]}`), and because raw JSON is **43.4 MB** it is committed **gzipped** as [plume_2024_2025.json.gz](webapp/dashboard/static/dashboard/plume_2024_2025.json.gz) (**16.2 MB**, user-chosen over raw/trim). [app-plan.js](webapp/dashboard/static/dashboard/app-plan.js) fetches the `.gz` as bytes and **decompresses client-side via `DecompressionStream`**, detecting the gzip magic `0x1f 0x8b` so it works whether the server serves raw `.gz` (Vercel) or auto-decompresses it (Django dev sets `Content-Encoding: gzip`). It then **counting-sorts** all points into per-hour buckets backed by typed arrays (`stepStart`/`stationIdxByPair`/`pmByPair`) so a frame's points are built on demand in O(active sensors) — never ~4.7M JS objects at once (~38 MB steady). Slider = 17,544 hourly steps, clock shows date+hour UTC, auto-opens on the **peak-median smoke hour = 2025-06-06 17:00 UTC (299 sensors)** — the major June-2025 wildfire smoke event. Initial map fit trims ~2.5% lat/lon outliers (Québec sensors reach ~62°N) to frame the dense southern cloud. Template/labels updated 2023→2024–2025, daily→hourly, Day→Hour. **Regenerate:** `PURPLEAIR_API_KEY=… python scripts/gen_plume_2024_2025.py --fetch-coords --write` (re-run for a new download dir; `--estimate` measures size with placeholder coords, no key). Browser-verified (headless Chrome) end-to-end incl. both decode branches; `manage.py check` clean. `plume_2023.json` remains committed (unused by the tracker now, still the coord fallback).
- **Research paper is now the published source of truth — all site stats reconciled (branch `feature/paper-source-of-truth-stats`, 2026-07-18):** User supplied the finished paper `NAS_TeamSmoke_BeforeTheSkyTurnsOrange_Paper-1.pdf` (Bui & Zander, "Before the Sky Turns Orange") and asked to make it the source of truth, replacing the older `CLEAR_Methodology_ScienceFair Ver#1` figures. **New canonical numbers (paper Tables 1–4), which SUPERSEDE the methodology-PDF figures:** 36M+ hourly PM2.5 obs; **479 stations analyzed = 348 NAPS + 131 U.S. EPA**, reduced by screening (r ≥ 0.30, p < 0.001, N ≥ 100) to a **56-station operational 360° network**; 2003–2023 = **development** set (May–Sep), 2024 = **out-of-sample validation** (the ONLY reported accuracy). **2024 validation:** 87.5% accuracy, 100% sensitivity (0 missed), 66.7% specificity, 24.7 h avg / 87 h max lead, 8 events (5 TP · 2 TN · 1 FP · 0 FN). Toronto peak 241 µg/m³ (Jun 28 2023). Alert bands (Table 2): Low <20, Moderate 20–60, High 61–80, Very High 81–120, Extreme >120. **The old headline figures (90.9% accuracy, 15.7 h avg, 33 events, "6–48 h", 440 stations / 309 NAPS, "162 active", "97.8% precision") do NOT appear in the paper and were replaced everywhere.** User decisions (2026-07-18): keep the three-rule structure (dashboard table retitled "2003–2023 Development Breakdown"; its 90.9%/15.7h kept only as development-set detail) but the **headline validation is now the paper's 2024 out-of-sample** (new "Independent Validation — 2024 Out-of-Sample" card added to the Results section); keep the 4-target-city product framing (Toronto = validated); station headline = "479 analyzed + 56 operational"; repoint all "source of truth" citations to the paper. **Surfaces edited:** `landing.html` (hero/stat-strip/scale/bento + example-note + footer credit), dashboard `index.html` Research tab (summary/purpose/methodology/data-sources/results/impact + citations), `README.md` (tagline, alert bands, validation table, source-of-truth pointer, selection-criteria heading), `AGENTS.md` tagline. **NOT touched (deliberately):** `evaluate.py` alert engine + its methodology-PDF code comments (validated core; the methodology PDF remains the *implementation* reference for band/lead-time definitions), the live station catalog (`bundled_stations.json`, still ~201), and the API-docs example `"count": 162`. Verified: `manage.py check` clean; `/` and `/dashboard/` both HTTP 200 with every new figure in the served HTML and the old ones gone (except the intentional dev-table). Reading the PDF needed a one-off `pip install pypdf` on the Win box (no poppler for the Read tool's PDF render).
- **Wind integration for the smoke early-warning model (branch `feature/wind-integration`, 2026-07-19):** A prior session that was building this **crashed and saved NOTHING** — the `feature/wind-integration` branch sat at `a2c84fc` (main's PR#22 merge) with **zero commits**, and no wind code existed anywhere (grep for `open-meteo`/`wind_speed` was empty). Reconstructed from intent. **User decisions (2026-07-19):** historical/training wind = **Open-Meteo ERA5 archive**, live/forecast wind = **ECCC** (deferred, reuses the `eccc_ingest` grib pipeline); make the wind pipeline **asset-generic** so it serves BOTH the 2024-2025 PurpleAir asset and the multi-year NAPS asset. **The models these feed** (`scripts/poc_smoke_model.py` numpy MLP, `scripts/train_smoke_lstm.py` PyTorch LSTM, `scripts/gen_naps_hourly.py` multi-year NAPS hourly puller) were **also untracked/uncommitted** before the crash — all committed together on this branch. They predict Toronto-core median PM2.5 ≥ 35 µg/m³ H hours ahead from the surrounding field split into **8 fixed compass sectors**; wind's job is to tell the model *which* sector is actually upwind and how fast smoke advects in. **Open-Meteo ERA5 archive contract (verified live):** `GET https://archive-api.open-meteo.com/v1/archive?latitude&longitude&start_date&end_date&hourly=wind_speed_10m,wind_direction_10m&timezone=GMT&wind_speed_unit=ms` → `hourly.{time[ISO 'YYYY-MM-DDTHH:MM' UTC], wind_speed_10m[m/s], wind_direction_10m[deg the wind blows FROM]}`, index0 = start_date 00:00Z, **no API key** (non-commercial), free. **`wind_direction_10m` is meteorological (direction wind comes FROM) = the bearing of the upwind sector as seen from Toronto.** [scripts/gen_wind_hourly.py](scripts/gen_wind_hourly.py) reads a PM asset's `t0`/`n_steps` only, then places each returned hour at index `(utc-t0)/3600` → wind asset with identical timeline (chunked by calendar year). [scripts/wind_features.py](scripts/wind_features.py) (pure numpy, has a no-network `_selftest`) builds **5 causal features**: `w_logspeed`, `w_dir_sin/cos`, `w_upwind_pm` (log1p of Σ cos-alignment·sector-PM), `w_upwind_flux` (× speed). Wired into both models via **additive `--wind PATH`** (off by default; PoC does a with/without ablation in one run, appended AFTER the PM+seasonality block so the log1p slice never touches wind). **Verified:** self-test green; Toronto 2024-2025 wind fetched at 100% hourly coverage (51 KiB gz); PoC no-wind rows byte-identical to baseline; **LSTM+wind lifted validation ROC-AUC 0.906→0.944, PR-AUC 0.205→0.371** — but neither model beats the **persistence baseline** (Toronto's own current PM; test ROC-AUC 0.916/PR-AUC 0.675) on the extreme June-2025 test tail, which motivates training on the NAPS multi-event timeline. **Isolated:** reads only committed static assets; never touches `evaluate.py`, the alert engine, `CachedResult`, or `/plan/`. Local data/build artifacts gitignored: `naps_hourly/`, `wind_hourly/` (regen wind with `python scripts/gen_wind_hourly.py --asset <pm-asset>`). **NAPS multi-year timeline — DONE end-to-end (2026-07-19).** `naps_hourly/naps_pm25_hourly.json.gz` (t0 2003-01-01, `n_steps` 201,624, 127 stations, **13 within 25 km of Toronto**, 192,654 usable Toronto-median hours = 95.5%) already existed; generated the aligned wind with `python scripts/gen_wind_hourly.py --asset naps_hourly/naps_pm25_hourly.json.gz` → `wind_hourly/wind_naps_pm25_hourly.json.gz` (548 KiB, **100% hourly coverage** 2003–2025). Ran the LSTM with/without wind (`scripts/train_smoke_lstm.py --asset naps_hourly/naps_pm25_hourly.json.gz [--wind wind_hourly/wind_naps_pm25_hourly.json.gz] --seed 0`; script defaults `--tor-radius 25 --min-tor 3` are well-supported, no radius guessing). **Result on the 2020–2024 test tail (base rate 0.7%): Persistence ROC-AUC 0.897 / PR-AUC 0.210; LSTM no-wind 0.948 / 0.322; LSTM +wind 0.950 / 0.351.** So training on the NAPS **multi-event** history achieves what the PurpleAir-only 2024-2025 tail could not — **both LSTM variants now beat persistence**; wind adds a modest-but-real lift concentrated on the honest rare-event metric (PR-AUC +0.029, best val-AUC 0.927→0.936). Chronological val window (2016–2020) has only 42 positives, so early-stopping is a touch noisy — inherent to the split, reported honestly. **Per-horizon sweep (H=6/12/24/48 h, seed 0, same test tail, 2026-07-19):** the LSTM beats persistence on **ROC-AUC at every lead**, and its margin over persistence **widens with lead time** (ROC LSTM−persist ≈ +0.03 @6h → +0.05 @12h → +0.10 @24h → +0.13 @48h) as persistence decays. **Wind's lift concentrates at long lead** — negligible at 6 h (ΔPR +0.001; the current field already suffices) but largest at 48 h (ROC 0.807→0.851, PR 0.050→0.101 with wind), exactly the advection prediction that the upwind sector matters more the further ahead you forecast. Caveats reported honestly: PR-AUC on 272 rare positives is high-variance (H=24 wind ΔPR −0.011), and at the extreme 48 h lead persistence's PR-AUC (0.129) still edges the model (0.101) — the task is genuinely hard that far out. (Sweep reused the validated `--horizon` flag; no model-code change.) **NOT yet done:** ECCC live/forecast wind (the deployment half). **ECCC wind scope (verified live on the Datamart 2026-07-19, do not guess):** wind comes from the **RDPS weather model (`model_rdps/10km`), NOT the AQ model** — it publishes the **identical `RLatLon0.09` grid + `{stamp}_MSC_RDPS_{Var}_{Level}_..._PT{FFF}H.grib2` filename convention** as RAQDPS, so `_GridSampler` + the stamp-based discovery are reused verbatim. Sample **`_MSC_RDPS_WindSpeed_AGL-10m_`** (m/s) + **`_MSC_RDPS_WindDir_AGL-10m_`** (deg) — these map **1:1** to the Open-Meteo `wind_speed_10m`/`wind_direction_10m` the training side uses, so `wind_features.build_features` consumes them with **zero conversion** (WMO `WindDir` = direction the wind blows FROM = meteorological = matches Open-Meteo; confirm on first CI grib read). `WindU`/`WindV_AGL-10m` exist as a components alternative. **RDPS runs = 00/06/12/18** (probe saw 00, 06); forecast hours `000..N`, `000` = analysis. **⚠️ Path:** must use the NEW date-partitioned Datamart `https://dd.weather.gc.ca/{YYYYMMDD}/WXO-DD/model_rdps/10km/{HH}/{FFF}/` (scan today→yesterday, pick by filename stamp — mirror `smoke_pi/collector.py`'s `discover_firework`); the `/today/` base still hardcoded in `scripts/eccc_ingest.py` is DEAD (fix there too when next touched). **Storage contract (`webapp/dashboard/views/eccc.py`):** extend `_KIND_TO_KEY` with wind kinds (e.g. `wind_analysis`→`eccc_wind_analysis`, `wind_forecast`→`eccc_wind_forecast`) — a dict one-liner; payload shape is already generic (`points[sid][series]`), read back at `GET /api/eccc/?kind=wind_analysis|wind_forecast`. Grib read stays **CI-only** (eccodes/pygrib not on Vercel or the Win box). **ECCC wind ingest — BUILT (2026-07-19):** new self-contained [scripts/eccc_wind_ingest.py](scripts/eccc_wind_ingest.py) (heavy deps imported LAZILY inside the sampler so it imports/tests off-CI like `smoke_pi/collector.py`; the point catalogue is duplicated from `eccc_ingest.py` with a keep-in-sync note — same convention as `ONTARIO_BBOX` — because `eccc_ingest.py`'s module-top pygrib import makes it un-importable off-CI). It discovers the latest RDPS run on the NEW date-partitioned path, samples `WindSpeed`/`WindDir_AGL-10m` at the **241-point catalogue** (201 stations + 4 cities + 36 far-field, id-for-id matching the PM ingest), and POSTs `wind_analysis` (hour 000) / `wind_forecast` (000..N). `webapp/dashboard/views/eccc.py` `_KIND_TO_KEY` extended with the two wind kinds (+ error messages made generic); new workflow [.github/workflows/eccc-wind-ingest.yml](.github/workflows/eccc-wind-ingest.yml) — SEPARATE from the PM ingest (so the still-broken PM `/today/` path can't block wind), every 3h off-peak minute :27, `--analysis --forecast --skip-if-current`. **Verified locally without pygrib:** discovery picks the newest run by filename stamp (saw 06Z>00Z) and synthesises forecast URLs that HEAD-200 at h0/24/48 (~0.85 MB each → forecast ≈ 98 files / ~83 MB, analysis 2 files); the full POST→`CachedResult`→GET round-trip works and rejects bad kinds; `manage.py check` clean. **Only the pygrib grib READ is CI-only** (reuses the already-validated `_GridSampler`; sampling never ran on the Win box). Handy: `python scripts/eccc_wind_ingest.py --discover-only` (no deps, lists live URLs). **Still deferred beyond ingest:** actually feeding live wind into a deployed prediction needs both a deployed model and the fusion-into-`evaluate.py` methodology decision.
- **Live wind-aware Toronto smoke forecast — BUILT (branch `feature/live-smoke-forecast`, 2026-07-19):** deploys the validated NAPS LSTM (+wind) as a **read-only** product surface — `GET /api/forecast/` + a dashboard "Toronto Smoke Forecast" card showing P(Toronto-core median PM2.5 ≥ 35 µg/m³) at **6/12/24/48 h**. **ISOLATED:** reads `CachedResult(key="latest")` + the stored ECCC wind READ-ONLY, writes only its own keys; never touches `evaluate.py`, the 3-rule engine, or `key="latest"`. It is a forecast, **explicitly not an alert** (surfaced in-UI). **Architecture (spec: [docs/superpowers/specs/2026-07-19-live-smoke-forecast-design.md](docs/superpowers/specs/2026-07-19-live-smoke-forecast-design.md)):** (1) `train_smoke_lstm.py` refactored to expose `train_model()` — behaviour-preserving, verified `main` still reproduces 0.950/0.351 @H=12 — so the deployed weights come from the EXACT training path (no drift). (2) [scripts/export_smoke_model.py](scripts/export_smoke_model.py) trains all 4 horizons and freezes them to committed [webapp/dashboard/services/smoke_model.json](webapp/dashboard/services/smoke_model.json) (~1.2 MB: LSTM+head weights, train-fit μ/σ, feature order, **+ a torch reference prob per horizon**). (3) [services/smoke_forecast.py](webapp/dashboard/services/smoke_forecast.py) is a **pure-numpy** LSTM forward + feature build (**no torch → runs on the slim Vercel lambda**); geometry/wind-feature math duplicated from `poc_smoke_model.py`/`wind_features.py` with keep-in-sync notes (scripts/ isn't deployed). **VERIFIED numpy==torch to <1e-9** on the reference via `verify_against_reference()` (the gate; it caught a real bug — `float()` on a size-1 numpy array raises in numpy 2.x, needs `[...][0]`). (4) [views/forecast.py](webapp/dashboard/views/forecast.py): `POST /api/refresh/forecast/` (CRON_SECRET-gated, CSRF-exempt) reads the live field + ECCC wind, appends the current hour's raw feature row to a rolling 24-row buffer (`CachedResult(key="smoke_fc_buffer")`, dedup by UTC hour), and scores once full → `CachedResult(key="smoke_forecast")`; `GET /api/forecast/` serves it. **GOTCHA: `CachedResult.key` is `max_length=20`** — `"smoke_forecast_buffer"` (21) overflows, so the buffer key is `"smoke_fc_buffer"`. (5) dashboard card + [app-forecast.js](webapp/dashboard/static/dashboard/app-forecast.js) (4 bars, warming-up state, caveat; best-effort like `app-fusion.js`). **WARM-UP:** the model needs 24 h of history the app never stored, so right after deploy the card shows "warming up (N/24 h)" for ~a day, then real numbers; backfill deferred (past ECCC analyses aren't stored). **CADENCE — external cron, NOT GitHub Actions:** point a free external cron (cron-job.org / UptimeRobot) at `https://clear25.xyz/api/refresh/forecast/` with header `Authorization: Bearer <CRON_SECRET>` **hourly** (one buffer row/hour; per the private-repo Actions-budget lesson). **Domain-shift caveat (honest, in-UI):** trained on NAPS reference stations, served on the WAQI live network. **Verified locally:** export faithful (per-horizon ROC/PR match the sweep exactly); numpy gate <1e-9; endpoint POST→buffer→score→GET round-trip; dashboard card browser-verified; `manage.py check` clean. **Regenerate the model:** `python scripts/export_smoke_model.py --asset naps_hourly/naps_pm25_hourly.json.gz --wind wind_hourly/wind_naps_pm25_hourly.json.gz` (needs the research torch deps; not the Vercel runtime). **To go live:** merge → Vercel auto-deploys `smoke_model.json` + endpoints; wire the external hourly cron; card warms up over ~24 h. **Still deferred:** fusing the forecast into the live alert engine (methodology decision, pending backtest).
- **UI Phase 4 foundation — Tailwind Play CDN removed + design tokens (branch `feature/ui-phase4-foundation`, 2026-07-19):** From a full design audit. (1) **The dev-only Tailwind Play CDN** (~350 KB runtime JS + inline config, on every `base_app.html` page) served only a handful of utility classes in `feedback.html` — removed, and `cdn.tailwindcss.com` dropped from the CSP `script-src`. **GOTCHA #1:** the CDN also injected **Preflight** (global reset) that base_app pages silently relied on (`style.css` has NO reset of its own) — replaced with a preflight-equivalent reset in `style.css` **scoped via zero-specificity `:where(.app-layout)`** + `body.app-shell`, so billing/settings/login (which load style.css but never loaded Tailwind) are pixel-untouched. First attempt used plain `.app-layout h4`-style selectors (0-1-1) which **beat component classes** (0-1-0) and broke heading/button styling — `:where()` is mandatory for resets here. **GOTCHA #2: the app's root font-size is 15px**, so Tailwind rem utilities computed to 18.75/11.25px (not 20/12) — utility replacements must be restated in **rem**, verified pixel-identical against live prod computed styles. `.hidden` is now a style.css utility (app-feedback.js toggles it). (2) **Foundation tokens** in `:root` (text roles/spacing/type/radii/shadows/`--focus-ring`/icon sizes; breakpoints 640/768/1024 documented); **site-wide `:focus-visible` brand ring** (`.sidebar-tab`/`.action-btn` use `all:unset` which killed native outlines; the whole 56 KB sheet had ONE focus rule); sidebar labels `#71717a`→`--text-secondary` (**#71717a is below AA on --bg — don't use it for text <18px**); `.action-btn` press/disabled/`.is-loading` states. (3) Forecast card de-inlined to `.fc-*` components. **Browser-pane testing gotchas (recur):** geometry probes lie before the pane paints (`clientWidth` 0, rAF never fires) — front the tab and force sync recalc with `void el.offsetHeight`; box-shadow reads catch `transition` at t=0 (looks transparent) — disable the transition or force reflow first; `el.focus({focusVisible:true})` triggers the ring programmatically. **Backlog (audit):** landing.html's 1,573-line embedded `<style>` (second design system), 10→3 breakpoints, ~75 inline styles (research/api_docs worst), api_docs+plan don't extend base_app.
- **UI Phase 4b sweeps (branch `feature/ui-phase4b-sweeps`, 2026-07-19):** C3 contrast sweep completed (all 42 `#71717a` rules reviewed one-by-one: 23 promoted to `--text-secondary`/`--text-tertiary`; **`.tier-sep`/`.research-badge`/`.conclusion-item h3` use `var(--accent-color, #71717a)` — the severity-reactive accent feature, NOT a static gray; never "fix" them**). Breakpoints consolidated in `style.css` (900×2→1024 merged, 600→640; **500/400 kept deliberately** — they stack cards/hide the table distance column; merging up degrades 401–640px phones). `research.html` 21 inline styles → 0 (badges → canonical `.alert-badge[data-level]` + new `--lg`; **`.research-card p` (0-1-1) overrides plain component classes — research text components need `.research-card`-scoped 0-2-0 selectors**; `.key-stats` modifiers need BEM double-class to beat the later base rule; one inline style was silently reverting the C3 fix — inline styles always win, another reason to purge them). Landing: `--text-faint` 0.42→0.55 (9 functional-text rules were below AA) + first `:focus-visible` rule on the page. **Testing gotchas:** computed `grid-template-columns` can return un-resolved `repeat(N, 1fr)` — don't count columns by splitting on spaces; `el.focus({focusVisible:true})` may be declined by the per-page modality heuristic (worked on dashboard, declined on landing) — verify via CSSOM + a proven render of the same pattern elsewhere.
- **Forecast→alert fusion backtest — the gate says NOT YET (branch `research/forecast-alert-backtest`, 2026-07-19):** [scripts/backtest_smoke_alerts.py](scripts/backtest_smoke_alerts.py) replays the EXACT deployed artifact (`smoke_model.json` weights + deployed μ/σ; bit-exact sanity check against the exported reference) over the NAPS timeline as a hypothetical warning trigger. Method: episodes = Toronto-core median crossing ≥35 after ≥6 clean hours; θ per horizon chosen ONLY on the 2016–2020 val window (just 16 episodes); 2020–2024 test tail (32 episodes) touched once; persistence baseline val-tuned identically. **Test results: the model buys real lead (H=24: 19.4 h vs persistence 11.6 h; H=48: 37 h vs 19 h) but fires 2.5–4× the false episodes (46 vs 18 @H=24; 80 vs 18 @H=48), detects slightly fewer episodes (22/32 vs 24/32), and H=12's val-chosen θ collapsed on test (10/32) — single-threshold selection on 16 val episodes is unstable.** **DECISION (evidence-based): do NOT fuse into the 3-rule engine as a trigger; keep the forecast read-only.** Paths that could reopen the gate: smoke-specific episode labels (the ≥35 crossings include non-smoke winter events the 3-rule engine never targeted), precision-constrained or cross-validated θ selection, probability calibration, or requiring model+persistence agreement. Full numbers in [scripts/backtest_results.json](scripts/backtest_results.json).
- **Per-year PurpleAir sensor-index discovery for the download tool (branch `feature/purpleair-sensor-discovery`, 2026-07-19):** New [scripts/gen_sensor_indexes.py](scripts/gen_sensor_indexes.py) produces the sensor-index lists to paste into the PurpleAir "Download Data" tool, one file per year (`sensor_indexes/sensors_<Y>.txt`, gitignored). Built to backfill PurpleAir 2021–2023 (we already had 2024/2025) for a planned **3-source ML model (NAPS + PurpleAir + FireWork) + wind, 2021–2025**. **Key discoveries (verified live via the user's own key):** (1) the reason a manual download-tool pull only shows ~500 sensors is that the tool/`/v1/sensors` defaults to a ~1-week `max_age`, i.e. **currently-online sensors only** — that ~500 was an *undercount*, not the true count. To capture historical sensors (a 2021 sensor is usually offline now) you MUST pass **`max_age=0`** (all-time roster). (2) The raw roster is huge (**6,478** all-time in the ON+QC box) because it includes indoor + dead sensors — but `roster_size` is an intermediate print, NOT what's written. Filter to **outdoor only** (`location_type=0`; indoor units measure indoor air, wrong for ambient smoke) and **`confidence≥90`** (PurpleAir's own 2-channel QA) → with `--max-age 2592000` (30d) the per-year outdoor+QA counts are 2021:429, 2022:590, 2023:1027, 2024:1529, 2025:2104. The rising trend is **real network growth**, not a bug. **Region = ON+QC footprint matching the existing 2024/2025 extent** (observed lat 42.07–62.48, lon −94.55–−57.20 → bbox nwlat 63.5/nwlng −96.0/selat 41.5/selng −56.5), so all five years share one footprint. **Download-cost ("PurpleAir points") levers:** history is billed per row (discovery is cheap); use **May–Sep only** (the methodology's analysis window, ~58% fewer rows, loses no smoke events) and don't re-pull years you already have. **Download date convention (match existing files):** start `YYYY-01-01` → end `YYYY-12-31`, **60-Minute (hourly)** average; those CSVs carry only `time_stamp,pm2.5_atm` (no CF=1/RH → no EPA correction, same caveat as the 2024/2025 asset). **FireWork history caveat:** the model grids are NOT publicly archived (see the `smoke_pi` note) — the user states they can source it; verify it covers 2021–2025 before treating it as a training feature. Endpoint identical to `purpleair_history.py` discovery; needs `PURPLEAIR_API_KEY` in env.
- **Plume tracker rebuilt on the FINALIZED EPA-corrected 2021–2025 record (branch `feature/plume-finalized-2021-2025`, 2026-07-25):** `/plan/` now replays `pm2.5_epa_corrected` from `C:\Users\rybot\Desktop\ai_model_data_important\PurpleAir<YEAR>_calibrated_merged_with_locations.csv` (5 years, outside the repo, ~2.5 GB; see that folder's `HANDOFF_PurpleAir.md`). This **supersedes** the uncorrected `plume_2024_2025.json.gz` as the tracker's source — the old downloads had only `pm2.5_atm` (no CF=1/RH) so no EPA/Barkjohn correction was possible and values run ~2× hot. The finalized files also carry `latitude`/`longitude` columns, so **[scripts/gen_plume_finalized.py](scripts/gen_plume_finalized.py) makes ZERO network calls** (the 2024/2025 generator needed a PurpleAir metadata call to recover coords). **DO NOT delete `plume_2024_2025.json.gz`** — it is the default input for `scripts/poc_smoke_model.py` and `scripts/train_smoke_lstm.py`; deleting it breaks the ML pipeline. `plume_2023.json` (old daily asset) is likewise still the coord fallback for `gen_plume_2024_2025.py`. **⚠ WHITENOISE .gz SHADOWING (hit and fixed 2026-07-25 — assets are named `plume_finalized_<Y>.json.gz` for this reason, do NOT shorten them):** WhiteNoise serves static files in production and reads `X.gz` as the gzip *variant* of `X` whenever a file named `X` also exists — `whitenoise.base.add_file_to_dictionary()` returns early for the variant, so the `.gz` URL is **never registered and 404s**. The first cut named the assets `plume_<Y>.json.gz`; four years worked and **2023 alone 404'd in production** because `plume_2023.json` sits beside it. **`DEBUG=True` HIDES THIS COMPLETELY** — WhiteNoise runs in autorefresh mode and resolves through the staticfiles finders instead of the prebuilt file dict, so `runserver` serves the file happily and a dev-browser check passes. **Any browser verification of a static asset on this project is therefore not evidence about production.** `gen_plume_finalized.py:assert_no_whitenoise_shadow()` now fails the build if any `.gz` output has a same-stem sibling, and it is covered by `--selftest`. The frontend also takes each year's filename from the manifest's `file` field instead of templating a URL, so naming stays a generator concern. **Measured scope (full streaming scan, not estimated):** 24,880,549 clean rows, 1,742 unique sensors, lat 41.50–62.20 / lon −95.97 to −57.92 — and **only 227 of 1,742 sensors (13%) are in ON+QC**, so the old province clip was discarding 87% of the network, i.e. exactly the upwind US Midwest/Great Lakes coverage that shows smoke *arriving*. `clipRegion` is now off and `PROVINCE_POLYGONS` is repurposed from a clip mask to a reference `L.polyline` outline. **ENCODING — the key enabler:** measured on real 2022 data, absolute `[h,pm,h,pm]` (what all older assets use) costs **3.52 gz-B/pt** vs **1.22 for delta `[h₀,pm,Δh,pm]`** (columnar 1.13, binary varint 1.01). Delta is a **2.9× win for a one-line change on each side** and is what lets all 12 months of all 5 years ship (26.7 MB total: 1.6/2.3/4.8/7.0/11.0 MB) instead of fire-season only; at the old encoding this would have been 83.5 MB. Per-year `.gz` + `plume_index.json` manifest (peak hour, bbox, counts, trimmed global view), lazily fetched by a year selector. **Frontend gotchas fixed:** the old `canvasCache` grew unbounded (a latent leak — 8,760 hours/year); the index now uses `Uint16Array` station-idx + `Uint16Array` pm×10 (4 B/pt, so 2025's 10.2M readings hold at ~41 MB not ~81 MB) and **`CachedResult.key`-style limits don't apply here, but the 65,535-station Uint16 ceiling does — both generator and frontend refuse rather than wrap.** **Rendering — SENSOR DOTS ONLY, by explicit user decision (2026-07-25). There is no interpolated surface; do not add one back.** Each reporting sensor is drawn as one `L.circleMarker` at its own coordinates, colored by its own reading, on the map's shared canvas (`preferCanvas:true`). Every pixel of colour is a real measurement at a real location and empty space means nobody was measuring — nothing is implied between sensors. History, so it is not re-proposed as a fresh idea: an IDW surface was built, then given a nearest-sensor opacity falloff (→ field of separate blobs), then summed Gaussian coverage kernels `1 − e^(−1.6·Σ)` at σ=0.55° (which *did* merge clusters into one continuous plume and fade lone sensors) — the user rejected the soft look, then rejected the flat-alpha surface too ("bubbles"). All of that machinery (`renderFrame`/`buildBuckets`/`sizeGrid`/frame cache/`imageOverlay`) is DELETED. Dots are ~11× cheaper: **1.7 ms/frame vs 19 ms** on 2023, 3.8 ms on the densest year (2025, 1,556 sensors). The "show sensors" toggle was removed too — with dots as the only layer it just blanked the map. **Peak-hour vs peak-day differ and that's correct:** the manifest's peak is the highest across-sensor *hourly median* (2021-07-21T01Z, 2022-02-01T03Z, 2023-06-06T17Z, 2024-07-28T03Z, 2025-08-04T17Z); the QC summaries' peak *days* use a daily network *mean*, which ranks a few spiking sites above a broad blanket — the mean-vs-median distinction the handoff draws. 2021/2022 match directly; 2023 lands on the Jun 6–7 wave rather than Jun 25–30. **Verified:** generator `--selftest` green; an automatic post-write reconciliation gate decodes each `.gz` and asserts the point count equals the CSV read and hours are strictly increasing (all 5 exact); browser-measured 22 ms/frame (2023) and 32 ms/frame (2025) against a 100 ms playback interval, 0 console errors, 44 MB settled heap, and no leak across cycling all 5 years twice (515 MB observed was uncollected garbage that self-collected to 44 MB — **`performance.memory` without a forced GC will make you think you have a leak when you don't**). **Isolated:** no change to `evaluate.py`, the 3-rule engine, `CachedResult`, `views/plan.py`, or `/api/plan/refresh|frames|fusion/`. **Regenerate:** `python scripts/gen_plume_finalized.py --write` (add `--data-dir` if the folder moves).
