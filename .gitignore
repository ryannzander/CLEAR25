# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
.env

# Secrets (API key is read from env var on production)
data/config.json

#ai
.cursorrules/

# Data folder (research data stays local; config.example.json tracked for onboarding)
data/*
!data/config.example.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Django
db.sqlite3
*.log
staticfiles/

# Generator inputs (downloaded on demand by scripts/gen_bundled_stations.py)
scripts/aqs_sites.zip

# Pulled PurpleAir history (scripts/purpleair_history.py output; can be large)
purpleair_history/

# Per-year PurpleAir sensor-index lists (scripts/gen_sensor_indexes.py output)
sensor_indexes/

# PurpleAir coordinate cache (scripts/gen_plume_2024_2025.py --fetch-coords; local build artifact)
scripts/pa_coords_cache.json

# Claude
.claude/

# Capacitor / Node
node_modules/
ios/
android/

# AI
AGENTS.mds
/projdata
.cursorrules # cursor is no longer being used in the devlelopment of the project
# NAPS download for testing (not committed)
naps_2023/

# NAPS multi-year hourly pull (scripts/gen_naps_hourly.py; large local training data)
naps_hourly/

# Open-Meteo wind assets (scripts/gen_wind_hourly.py output; local build artifact,
# regenerate with: python scripts/gen_wind_hourly.py --asset <pm-asset>)
wind_hourly/

# Smoke collector output (Pi runtime data; keep the folder)
smoke_pi/web/data/*
!smoke_pi/web/data/.gitkeep

# ERA5 gridded wind download (scripts/download_era5_wind.py output; raw reanalysis,
# far too large to commit - regenerate with: python scripts/download_era5_wind.py)
era5_wind/
