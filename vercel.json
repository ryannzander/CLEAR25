{
    "version": 2,
    "buildCommand": "bash build_files.sh",
    "builds": [
        {
            "src": "webapp/ews/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "50mb",
                "excludeFiles": "data/**,**/tests/**,**/__tests__/**,**/*.test.py,**/test_*.py"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "webapp/ews/wsgi.py"
        }
    ],
    "crons": [
        {
            "path": "/api/refresh/",
            "schedule": "*/30 * * * *"
        }
    ]
}
